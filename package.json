{
  "name": "podhouse",
  "version": "0.1.0",
  "description": "The best way to listen your favorite podcasts.",
  "private": true,
  "author": "Leonardo Maldonado <leonardomso11@gmail.com>",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "app:start": "yarn workspace @podhouse/app start",
    "app:build": "yarn workspace @podhouse/app build",
    "app:test": "yarn workspace @podhouse/app test",
    "app:lint": "yarn workspace @podhouse/app lint",
    "app:format": "yarn workspace @podhouse/app format",
    "app:relay": "yarn workspace @podhouse/app relay",
    "web:dev": "yarn workspace @podhouse/web dev",
    "web:start": "yarn workspace @podhouse/web start",
    "web:build": "yarn workspace @podhouse/web build",
    "web:test": "yarn workspace @podhouse/web test",
    "web:lint": "yarn workspace @podhouse/web lint",
    "web:format": "yarn workspace @podhouse/web format",
    "web:relay": "yarn workspace @podhouse/web relay",
    "server:start": "yarn workspace @podhouse/server start",
    "server:build": "yarn workspace @podhouse/server build",
    "server:test": "yarn workspace @podhouse/server test",
    "server:serve": "yarn workspace @podhouse/server serve",
    "server:lint": "yarn workspace @podhouse/server lint",
    "server:format": "yarn workspace @podhouse/server format",
    "lint:staged": "lint-staged",
    "build": "yarn workspace @podhouse/web build & yarn workspace @podhouse/server build",
    "lint": "yarn workspace @podhouse/web lint & yarn workspace @podhouse/server lint",
    "test": "yarn workspace @podhouse/web test & yarn workspace @podhouse/server test",
    "update-schema": "yarn workspace @podhouse/server update-schema"
  },
  "devDependencies": {
    "husky": "^4.3.0",
    "lerna": "^3.22.1",
    "lint-staged": "^10.3.0",
    "prettier": "^2.1.1",
    "typescript": "^4.0.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn lint:staged"
    }
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "yarn update-schema",
      "yarn lint",
      "git add"
    ]
  }
}
